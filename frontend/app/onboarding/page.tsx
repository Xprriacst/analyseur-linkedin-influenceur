/**
 * URI canonique du tunnel d'essai builders (SaaS et freelance).
 * L'implémentation vit dans `OnboardingFunnelPage` — `/founders` réexporte
 * le même composant (liens déjà en circulation).
 */
export { default } from "./OnboardingFunnelPage";
