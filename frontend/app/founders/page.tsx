/**
 * Alias historique : `/founders` reste servi pour les liens déjà en circulation.
 * Le tunnel canonique est `/onboarding` (SaaS et freelance).
 */
export { default } from "../onboarding/OnboardingFunnelPage";
