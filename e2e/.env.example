# Copier en .env (chargé par Playwright via dotenv si tu l'ajoutes) ou exporter dans le shell.
# Cible des tests (défaut : site dev déployé)
E2E_BASE_URL=https://lkd-outreach-dev.netlify.app
# Compte de test Supabase (parcours authentifiés)
E2E_EMAIL=qa.playwright@lkd-outreach.app
E2E_PASSWORD=Lkd!Test2026
